services:
  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "${HOST_PORT:-5173}:${HOST_PORT:-5173}"
    env_file:
      - .env
    volumes:
      - ./frontend:/frontend
      - /frontend/node_modules
  
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend
    ports:
      - "${VITE_API_PORT:-8060}:${VITE_API_PORT:-8060}"
    env_file:
      - .env
    volumes:
      - shared_gen:/shared_gen
    depends_on:
      - queue_db
  
  queue_db:
    image: redis:7
    container_name: queue_db
    ports:
      - "6379:6379"
  
  worker_generate:
    build: 
      context: ./worker_generate
      dockerfile: Dockerfile
    container_name: worker_generate
    env_file:
      - .env
    volumes:
      - shared_gen:/shared_gen
    depends_on:
      - queue_db

volumes:
  shared_gen: